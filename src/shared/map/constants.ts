import type { GeoPoint } from '../location/location-store';

/** Kuala Lumpur city centre — go-ride-driver-app's HomeMap fallback. */
export const KL_CENTER: GeoPoint = { lat: 3.139, lng: 101.6869 };
