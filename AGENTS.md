code base to simulate and visualise the go ride cab booking
it simulates driver and user,

we can see the websocket communication / redis communication / trip request state / etc


good visual with map integration for both driver and user,

there is a split screen driver side / user side

one user and multiple driver , we can chose the driver locations and user location, selectable from map by clicking on google map

then the user will start calling the apis from login to booking .